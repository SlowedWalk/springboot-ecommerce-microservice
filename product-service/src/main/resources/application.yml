spring:
  application:
    name: product-service
  datasource:
    url: jdbc:mysql://localhost:3306/product-service-db?createDatabaseIfNotExist=true&useSSL=false
    #    url: mysql://localhost:3306/product-service-db?createDatabaseIfNotExist=true&useSSL=false
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
#logging:
#  level:
#    web:
#      - debug
#      - info
#      - error
#      - warn
#      - trace
#      - fatal
server:
  port: 8080
#debug: true


